# بررسی پایداری Candidateهای Phase 3 (magnitude-gate) — نتیجه‌ی امیدوارکننده‌ی اولیه با تفکیک ریزتر رد شد

این سند نتیجه‌ی اعتبارسنجی عمیق‌تر چند candidate خروجی Phase 3 پایپ‌لاین research (`backend/src/research/classification_phase.py`) است: ترکیب‌هایی از یک سیگنال جهت‌دار (`base_signal`) با یک گیت نوسان مبتنی بر percentile (`magnitude_filter`) که در اجرای اولیه‌ی Phase 3 روی داده‌ی واقعی BTC/USDT (۲۰۲۶-۰۱-۰۹ تا ۲۰۲۶-۰۷-۰۹، تایم‌فریم 1h) بهبود win-rate قابل‌توجهی (+۲.۹ تا +۶.۶ درصد) نسبت به baseline نشان داده بودند.

سؤال این بررسی: آیا این بهبود win-rate یک ادج تکرارپذیر است، یا محصول یک بازه‌ی زمانی/آستانه‌ی خاص (overfitting)؟ برای پاسخ، ابزار جدید `backend/scripts/run_candidate_stability.py` ساخته شد که هر candidate را روی ۳ زیر-بازه‌ی زمانی متوالی، چند regime بازار، و ۵ آستانه‌ی percentile مختلف (p75–p95) دوباره می‌سنجد و متریک‌های معامله‌ای واقعی‌تر (Expectancy, Profit Factor, Avg Win/Loss, Max Drawdown, عملکرد بعد از fee/slippage تقریبی) را محاسبه می‌کند.

**این کل بررسی در لایه‌ی آماری research انجام شده — بدون تغییر در Decision Engine، Provider واقعی، یا execution.**

**نتیجه‌ی نهایی (بعد از پنج دور بررسی تکمیلی — horizon، جداسازی اثر گیت، نوع آستانه، تفکیک ریزتر، و سازگاری با پلتفرم):** در بررسی اولیه (۴ candidate با h=14/24) هیچ‌کدام بعد از کسر هزینه‌ی معامله (fee+slippage) در هر ۳ زیر-بازه سودده نماندند. وقتی بهترین candidate (`ema_compare_50_200 + atr_pct_percentile_200 @p90`) با horizon های بلندتر (h=48, h=72، ۳ زیر-بازه‌ی درشت) دوباره تست شد، نتیجه امیدوارکننده به نظر رسید — اما این نتیجه بعداً **رد شد**: با تفکیک ریزتر (۵ زیر-بازه به‌جای ۳) و محدود کردن horizon به مقداری که واقعاً با `max_bars_in_trade` پیش‌فرض پلتفرم (۴۸) سازگار است، معلوم شد الگوی سودده/زیان‌ده هر زیر-بازه مستقل از آستانه‌ی percentile تکرار می‌شود (یعنی مشکل از رژیم بازار است، نه انتخاب آستانه) و زیر-بازه‌ی holdout که در تفکیک درشت مثبت بود، در تفکیک ریزتر منفی شد. **نتیجه‌ی نهایی: این candidate رد می‌شود** — نه به‌خاطر هزینه‌ی معامله (که فرضیه‌ی اولیه بود)، بلکه چون خودِ ادج به مرزهای اختیاری تفکیک زیر-بازه حساس بود، یعنی یک artifact overfitting، نه یک الگوی بازار واقعی.

**پیگیری‌های هفتم تا نهم (ensemble، تایم‌فریم بزرگ‌تر، سیگنال order-flow):** سه فرضیه‌ی متوالی برای حل ناپایداری رژیمی، هر سه رد شد. (۷) ترکیب چند سیگنال جهت‌دار مستقل (majority-vote، مثل `Aggregator` واقعی) — رد شد چون هر ۶ سیگنال پایه از یک خانواده (trend-following) خطای همبسته دارند. (۸) تایم‌فریم بزرگ‌تر (4h/1d) برای فیلتر نویز رژیمی کوتاه‌مدت — رد شد، روی 4h نتیجه حتی بدتر از 1h بود. (۹) افزودن سیگنال با فرضیه‌ی بنیادین متفاوت (`volume_order_flow`/CMF، جریان سفارش نه جهت قیمت) — رد شد؛ این سیگنال هم در Phase 1 هیچ ادجی نداشت و در ensemble همان زیر-بازه‌ی زیان‌ده را عیناً تکرار کرد.

**بررسی نهایی زیرساخت:** بعد از چهار رد پیاپی روی دقیقاً همان زیر-بازه، احتمال باگ در pipeline تحقیق مستقیماً بررسی شد. نتیجه: **زیرساخت سالم است.** زیر-بازه‌ی «بد» در واقعیت یک روند صعودی خالص +۱۵٪ بوده (نه داده‌ی خراب)؛ علت واقعی ضرر این بود که whipsaw طبیعی سیگنال (۳۲.۵٪ بارها DOWN حتی وسط روند صعودی) gross edge را کوچک نگه می‌داشت (+۰.۲۰۳٪ هر معامله) و هزینه‌ی واقعی رفت‌وبرگشت معامله (۰.۳٪) همین ادج کوچک را بلعید. جزئیات در بخش «بررسی زیرساخت» پایین سند.

---

## روش

### ابزار و مفاهیم جدید

| مفهوم | فایل |
|---|---|
| متریک‌های معامله‌ای (Expectancy, Profit Factor, Avg Win/Loss, Max Drawdown, عملکرد net-of-fees) | [classification_phase.py](../../backend/src/research/classification_phase.py) — `TradeStats`, `_trade_pnls`, `_net_of_fees` |
| تقسیم به زیر-بازه، برچسب‌گذاری regime، sweep آستانه‌ی percentile با holdout، overlay فیلتر/سایزینگ ATR | [stability_phase.py](../../backend/src/research/stability_phase.py) |
| CLI اجرا | [run_candidate_stability.py](../../backend/scripts/run_candidate_stability.py) |

### قواعد ضد-overfitting به‌کاررفته

- **زیر-بازه‌ی سوم همیشه holdout است**: انتخاب «بهترین آستانه‌ی percentile» فقط روی دو زیر-بازه‌ی اول انجام می‌شود؛ زیر-بازه‌ی سوم فقط برای گزارش نهایی لمس می‌شود.
- **معیار تکرارپذیری**: candidate فقط وقتی «قابل تکرار» شمرده می‌شود که بهبود win-rate در حداقل ۲ از ۳ زیر-بازه بالای ۱.۰pp بماند و هیچ regime منفردی بیش از ۸۰٪ معاملات را در بر نگیرد (همان آستانه‌ی `MAX_SINGLE_REGIME_SHARE` که در `governance/candidate_evaluator.py` برای پذیرش رسمی candidate استفاده می‌شود).
- **هشدار ناپایداری رتبه (`rank_unstable`)**: اگر آستانه‌ی برنده در هر زیر-بازه‌ی train عوض شود (نه فقط عدد امتیاز)، این را صریحاً به‌عنوان نشانه‌ی overfitting گزارش می‌کند.
- **fee/slippage تقریبی**: هزینه‌ی رفت‌وبرگشت از `FillModel` پیش‌فرض واقعی پلتفرم (`load_default_fill_model()`) خوانده و از PnL هر معامله کسر می‌شود — یک تقریب flat-bps، نه شبیه‌سازی order-level واقعی.

### محدودیت‌های شناخته‌شده (باید هنگام خواندن اعداد در نظر گرفته شود)

- `max_drawdown_pct` در این ابزار روی **مجموع تجمعی (غیرترکیبی)** PnL درصدی هر معامله محاسبه می‌شود (sizing ثابت، بدون equity واقعی) — می‌تواند از ۱۰۰٪ هم بیشتر شود و معادل drawdown واقعی حساب (که در `validation/metrics.py::compute_outcome_metrics` محاسبه می‌شود) نیست؛ فقط ابزار مقایسه‌ی نسبی بین candidateهاست.
- سایزینگ حجم مبتنی بر ATR (`gate_plus_volatility_sizing`) یک شبیه‌سازی وزن‌دهی PnL است، نه معادل واقعی `execution/simulated_pricing.py::position_size` (که بر مبنای فاصله‌ی stop-loss واقعی کار می‌کند).

---

## نتایج: ۴ candidate روی همان بازه‌ی داده (۲۰۲۶-۰۱-۰۹ تا ۲۰۲۶-۰۷-۰۹)

| Candidate | پنجره‌های net-of-fees مثبت | پنجره‌های gross مثبت | رتبه‌ی آستانه پایدار؟ |
|---|---|---|---|
| `market_structure_bias_5 + atr_pct_percentile_200 @p70, h=14` | ۱/۳ | ۳/۳ | ✅ پایدار |
| `market_structure_bias_5 + bb_width_percentile_200 @p90, h=14` | ۱/۳ | ۲/۳ | ❌ ناپایدار |
| **`ema_compare_50_200 + atr_pct_percentile_200 @p90, h=24`** | **۲/۳** | ۳/۳ | ❌ ناپایدار |
| `market_structure_bias_5 + atr_pct_percentile_200 @p90, h=24` | ۱/۳ | ۲/۳ | ❌ ناپایدار |

### جزئیات بهترین candidate (`ema_compare_50_200 + atr_pct_percentile_200 @p90, h=24`)

| زیر-بازه | بهبود win-rate | تعداد معاملات | Expectancy (خام) | Profit Factor | Expectancy (net-of-fees) |
|---|---|---|---|---|---|
| ۱/۳ | +2.1pp | 253 | 0.131% | 1.08 | **-0.169%** |
| ۲/۳ | +14.1pp | 71 | 0.664% | 2.46 | +0.364% |
| ۳/۳ (holdout) | +11.1pp | 194 | 1.420% | 5.09 | +1.120% |

با این حال، sweep آستانه‌ی percentile برای همین candidate هشدار `rank_unstable` داد — یعنی اگر روی همان دو زیر-بازه‌ی اول آستانه‌ی «بهینه» انتخاب می‌شد، آن انتخاب بین زیر-بازه‌ها ثابت نمی‌ماند.

### پیگیری: sweep کامل horizon روی بهترین candidate (`ema_compare_50_200 + atr_pct_percentile_200 @p90`)

فرضیه‌ی اولیه («horizon بلندتر هزینه‌ی نسبی معامله را کم می‌کند») با تست h=48 و h=72 (علاوه بر h=14/24 قبلی) روی همان بازه‌ی داده تأیید شد — با یک اصلاح مهم در ابزار (اضافه‌شدن `horizons` سفارشی به `compute_forward_targets` در CLI، چون قبلاً فقط horizon های پیش‌فرض `(4,8,14,24)` ساخته می‌شد و horizon های بزرگ‌تر باعث خطا می‌شدند):

| horizon | پنجره‌های net-of-fees مثبت | net_expectancy هر پنجره | رتبه‌ی آستانه پایدار؟ |
|---|---|---|---|
| h=14 | ۱/۳ | -0.086%, -0.206%, +0.553% | ✅ پایدار (تصادفی، تنها مورد) |
| h=24 | ۲/۳ | -0.169%, +0.364%, +1.120% | ❌ ناپایدار |
| h=48 | ۳/۳ | +0.010%, +0.202%, +1.848% | ❌ ناپایدار |
| **h=72** | **۳/۳** | **+0.124%, +0.630%, +2.206%** | ❌ ناپایدار |

روند کاملاً یکنواخت و monotonic است: با افزایش horizon، هم بهبود win-rate در هر ۳ زیر-بازه مثبت می‌ماند (در h=72: +3.9pp, +12.9pp, +8.0pp) و هم net-of-fees expectancy در هر ۳ زیر-بازه به‌وضوح بهتر می‌شود. این نشان می‌دهد **بخش قابل‌توجهی از ضعف اولیه صرفاً محصول هزینه‌ی تراکنش روی horizon کوتاه بود، نه نبود ادج مسیر جهت‌دار (EMA 50/200)**.

### تست تعیین‌کننده: آیا ادج از خود سیگنال می‌آید یا از گیت percentile؟

چون `rank_unstable` مربوط به گیت بود نه به سیگنال، `ema_compare_50_200` را **بدون هیچ گیتی** (یعنی تمام سیگنال‌های خام EMA-crossover، بدون فیلتر نوسان) روی همان ۴ horizon و همان بازه‌ی داده دوباره تست کردیم:

| horizon (بدون گیت) | پنجره‌های net-of-fees مثبت | net_expectancy هر پنجره |
|---|---|---|
| h=14 | ۰/۳ | -0.162%, -0.306%, -0.170% |
| h=24 | ۰/۳ | -0.085%, -0.245%, -0.048% |
| h=48 | ۰/۳ | +0.069%, -0.185%, +0.173% |
| h=72 | ۰/۳ (۲/۳ مثبت) | +0.118%, -0.136%, +0.376% |

**نتیجه‌ی روشن: بدون گیت percentile، سیگنال خام EMA 50/200 در هیچ horizon ای در هر ۳ زیر-بازه net-of-fees مثبت نیست.** حتی در h=72 (بهترین حالت) یک زیر-بازه (پنجره‌ی ۲) هنوز منفی می‌ماند. این یعنی فرضیه‌ی «ادج از خود سیگنال جهت‌دار می‌آید، گیت فقط نویز اضافه می‌کند» **رد شد** — ادج واقعی از **ترکیب** سیگنال + گیت نوسان می‌آید، نه از هرکدام به‌تنهایی. مقایسه‌ی مستقیم (مثلاً h=72، پنجره‌ی ۱): با گیت +0.124% در برابر بدون گیت +0.118% — نزدیک‌اند، ولی پنجره‌ی ۲ تفاوت اصلی را نشان می‌دهد (با گیت +0.630% در برابر بدون گیت -0.136%). گیت percentile دقیقاً همان معاملاتی را حذف می‌کند که در پنجره‌ی ۲ ضررده بودند.

این پیام دوگانه است: از یک طرف تأیید می‌کند گیت واقعاً کار مفیدی انجام می‌دهد (فیلتر کردن معاملات بد)، نه صرفاً یک artifact بی‌اثر؛ از طرف دیگر همان هشدار `rank_unstable` باقی می‌ماند — یعنی مقدار *دقیق* آستانه‌ی percentile هنوز به بازه‌ی زمانی حساس است، حتی اگر خود ایده‌ی «گیت کردن روی نوسان بالا» درست باشد.

### پیگیری: آستانه‌ی absolute به‌جای rank-percentile نسبی — بدتر شد، نه بهتر

فرضیه‌ی مطرح‌شده در نسخه‌ی قبلی این سند این بود که شاید یک آستانه‌ی **absolute** ثابت روی `atr_pct` خام (نه رتبه‌ی نسبی به ۲۰۰ بار اخیر) پایدارتر از percentile نسبی باشد. برای تست، `sweep_percentile_thresholds` (که از قبل عمومی بود و فقط `filter_series >= threshold` می‌کند، مستقل از اینکه مقیاس آن ۰-۱۰۰ باشد یا خام) با آستانه‌های absolute برابر کوانتایل‌های ۷۵ تا ۹۵ توزیع واقعی `atr_pct` (۰.۷۸٪ تا ۱.۲۱٪) روی h=48 و h=72 اجرا شد:

| horizon | بهترین آستانه‌ی absolute (train) | avg_train_improvement | holdout_improvement | rank_unstable |
|---|---|---|---|---|
| h=48 | atr%≥1.05 | +18.6pp (train window دیگر منفی بود) | **-7.3pp** | ❌ |
| h=72 | atr%≥0.96 | +10.7pp | **-4.5pp** | ❌ |

نتیجه واضح و در جهت مخالف فرضیه است: **آستانه‌ی absolute به‌طور قابل‌توجهی بدتر و ناپایدارتر از rank-percentile نسبی عمل کرد** — نه‌فقط رتبه‌ی «بهترین آستانه» همچنان ناپایدار ماند، بلکه holdout در هر دو horizon منفی شد (در مقابل +۱۷.۹pp و +۹.۲pp با گیت percentile نسبی). دلیل محتمل: `atr_pct` خام یک سری **غیر-ثابت (non-stationary)** است — سطح مطلق نوسان BTC در طول زمان جابه‌جا می‌شود (رژیم‌های آرام/پرنوسان)، پس یک عدد ثابت که روی یک زیر-بازه کالیبره شده در زیر-بازه‌ی بعدی که کل سطح نوسان بازار عوض شده معنای متفاوتی پیدا می‌کند. گیت rank-percentile دقیقاً همین مشکل را با نرمال‌سازی نسبت به تاریخچه‌ی غلتان (۲۰۰ بار) حل می‌کند — و این خودش توضیح می‌دهد چرا علی‌رغم ناپایداری در *مقدار دقیق* آستانه، طرح کلی rank-percentile هنوز بهتر از جایگزین absolute است.

یک نشانه‌ی جانبی هم این را تأیید کرد: بهترین آستانه‌ی absolute در h=72 (atr%≥0.96) در یکی از زیر-بازه‌ها فقط ۳۰ معامله داشت (نزدیک به کف `MIN_TRADES_FOR_COMPARISON=30`) و پرچم تمرکز regime هم فعال شد — نشانه‌ی دیگری از بی‌ثباتی این رویکرد.

**نتیجه: سؤال باز #۱ از نسخه‌ی قبلی این سند بسته شد.** rank-percentile نسبی (نه absolute) رویکرد درستی است؛ مشکل باقی‌مانده صرفاً *کدام* آستانه‌ی percentile است، نه *نوع* آستانه.

### پیگیری: ۵ زیر-بازه به‌جای ۳ — ناپایداری از رژیم بازار می‌آید، نه از خود آستانه

فرضیه‌ی دیگر این بود که با ۳ زیر-بازه‌ی بزرگ، رتبه‌بندی آستانه‌ها نویزی است چون هر زیر-بازه شامل چند رژیم بازار مخلوط می‌شود. برای تست دقیق‌تر، همان sweep روی ۵ زیر-بازه (به‌جای ۳) روی h=48 و h=72 دوباره اجرا شد:

| horizon | best (train) | rank_unstable | الگوی per-window (۵ زیر-بازه) | holdout |
|---|---|---|---|---|
| h=48 | p75 | ❌ ناپایدار | window0: مثبت قوی، window1: **منفی**، window2: مثبت قوی، window3: **منفی** (در همه‌ی ۵ آستانه یکسان) | +0.8pp (ضعیف) |
| h=72 | p80 | ❌ ناپایدار | window0: مثبت قوی، window1: **منفی**، window2: مثبت قوی، window3: نزدیک صفر (در همه‌ی ۵ آستانه یکسان) | +2.8pp |

نکته‌ی کلیدی: با ۵ زیر-بازه، **الگوی مثبت/منفی هر پنجره مستقل از آستانه‌ی انتخابی است** — یعنی window1 برای هر ۵ آستانه (p75 تا p95) منفی است، نه فقط برای یکی. این نشان می‌دهد ناپایداری `rank_unstable` صرفاً از انتخاب آستانه‌ی «اشتباه» نمی‌آید؛ ریشه‌اش **رژیم بازار حاکم در هر زیر-بازه** است — این ترکیب سیگنال در برخی بازه‌های زمانی (window1, window3) ضرر می‌دهد، مستقل از این‌که آستانه‌ی percentile چقدر باشد. این یک یافته‌ی مهم‌تر و نگران‌کننده‌تر از خود ناپایداری آستانه است: مسئله «انتخاب آستانه‌ی بهتر» نیست، مسئله این است که **کل استراتژی در برخی رژیم‌ها/بازه‌های زمانی به‌طور ذاتی ضررده است**.

### پیگیری: سازگاری با `max_bars_in_trade` واقعی پلتفرم — h=72 در عمل قابل‌اجرا نیست

`backend/src/execution/simulated_exits.py:42` تأیید می‌کند که در اجرای واقعی، هر معامله‌ای که `bars_held >= max_bars_in_trade` برسد با `exit_reason="timeout"` به‌زور بسته می‌شود — و `max_bars_in_trade` پیش‌فرض در `config/settings.yaml` برابر **۴۸** است، دقیقاً برابر horizon نه ۷۲. یعنی معادل واقعی candidate با h=72 **هرگز در پلتفرم زنده رخ نمی‌دهد** مگر این پیش‌فرض صریحاً تغییر کند — نتایج h=72 در این سند فرضی‌اند (بازه‌ی افق آزاد در داده‌ی تاریخی)، نه چیزی که با تنظیمات فعلی پلتفرم قابل‌تکرار باشد.

برای بررسی نسخه‌ی واقعاً قابل‌اجرا، candidate با h=48 (که دقیقاً با `max_bars_in_trade` پیش‌فرض هم‌راستاست) روی همان ۵ زیر-بازه دوباره اجرا شد:

| زیر-بازه | بهبود win-rate | تعداد معاملات | Expectancy (خام) | Expectancy (net-of-fees) |
|---|---|---|---|---|
| ۱/۵ | +10.1pp | 146 | 1.761% | +1.461% |
| ۲/۵ | -12.6pp | 111 | **-1.617%** | **-1.917%** |
| ۳/۵ | +18.1pp | 37 | 0.328% | +0.028% |
| ۴/۵ | -7.4pp | 95 | 1.992% | +1.692% |
| ۵/۵ (holdout) | -9.1pp | 48 | **-0.366%** | **-0.666%** |

فقط **۲ از ۵ زیر-بازه** بالای حد نصاب پایداری ماندند، و مهم‌تر: **holdout (زیر-بازه‌ی آخر) خودش منفی است** — برخلاف نسخه‌ی ۳-پنجره‌ای قبلی این سند که holdout را مثبت گزارش کرده بود. این تفاوت مستقیماً از ریزدانه‌تر شدن تقسیم‌بندی می‌آید: بازه‌ی زمانی که در تقسیم ۳تایی داخل یک پنجره‌ی بزرگ و مثبت جا می‌شد، در تقسیم ۵تایی به یک زیر-بازه‌ی مجزا و منفی افتاده است.

**نتیجه: این یافته نسخه‌ی خوش‌بینانه‌ی h=48/h=72 (۳ زیر-بازه) را زیر سؤال می‌برد.** با تفکیک ریزتر (که واقعی‌تر است، چون رژیم بازار در بازه‌های کوچک‌تر همگن‌تر است) و با horizon ای که واقعاً با محدودیت‌های پلتفرم سازگار است (h=48، نه h=72)، این candidate دیگر به‌وضوح پایدار نیست.

---

## جمع‌بندی و توصیه

| # | یافته | severity | وضعیت |
|---|---|---|---|
| 1 | ۴ candidate اولیه (h=14/24) بعد از fee/slippage در هر ۳ زیر-بازه سودده نبودند | بحرانی برای تصمیم promotion | ⏳ رد شد در حالت h=14/24 |
| 2 | افزایش horizon به ۴۸ و ۷۲ ساعت net-of-fees expectancy را (با گیت، ۳ زیر-بازه‌ی درشت) مثبت و monotonic بهتر کرد | یافته‌ی اولیه‌ی امیدوارکننده — **بعداً با تفکیک ریزتر رد شد (ردیف ۶)** | ⚠️ نتیجه‌گیری زودهنگام بود |
| 3 | بدون گیت percentile، همان سیگنال در هیچ horizon ای net-of-fees پایدار نیست (h=72 هم فقط ۲/۳) | رد فرضیه‌ی «ادج فقط از سیگنال است» | ✅ تست شد و رد شد |
| 4 | آستانه‌ی absolute (به‌جای rank-percentile نسبی) امتحان شد و به‌طور واضح بدتر بود (holdout منفی در هر دو horizon) | رد فرضیه‌ی جایگزین absolute؛ rank-percentile نسبی درست‌تر است | ✅ تست شد و رد شد |
| 5 | با ۵ زیر-بازه (به‌جای ۳)، الگوی مثبت/منفی هر پنجره **مستقل از آستانه‌ی percentile انتخابی** است | ناپایداری از رژیم بازار می‌آید، نه از انتخاب آستانه | ✅ یافته‌ی تعیین‌کننده |
| 6 | با h=48 (تنها horizon واقعاً سازگار با `max_bars_in_trade` پیش‌فرض پلتفرم) و ۵ زیر-بازه، فقط ۲/۵ پایدار ماندند و **holdout منفی شد** | بحرانی — نتیجه‌ی خوش‌بینانه‌ی #۲ را نقض می‌کند | ✅ رد قطعی |
| 7 | h=72 در عمل در پلتفرم زنده قابل‌اجرا نیست چون `max_bars_in_trade` پیش‌فرض (۴۸) معامله را زودتر با `timeout` می‌بندد | نتایج h=72 این سند فرضی‌اند، نه قابل‌تکرار با تنظیمات فعلی | ✅ تأیید شد در کد (`simulated_exits.py:42`) |

**توصیه‌ی نهایی (تجدیدنظرشده): این candidate رد می‌شود.** نتیجه‌ی اولیه‌ی امیدوارکننده (ردیف #۲) صرفاً یک artifact تفکیک درشت بود: با ۳ زیر-بازه‌ی بزرگ، چند رژیم بازار متفاوت (برخی سودده، برخی زیان‌ده) داخل یک پنجره مخلوط می‌شدند و میانگین‌شان مثبت به نظر می‌رسید. وقتی تفکیک ریزتر شد (۵ زیر-بازه) و horizon به مقدار واقعاً قابل‌اجرا در پلتفرم (h=48، نه h=72) محدود شد، معلوم شد:

- الگوی سودده/زیان‌ده **مستقل از آستانه‌ی percentile** در هر زیر-بازه تکرار می‌شود — یعنی این ترکیب سیگنال در برخی بازه‌های زمانی/رژیم‌ها به‌طور ذاتی ضررده است، و هیچ انتخاب آستانه‌ای این را حل نمی‌کند.
- زیر-بازه‌ی holdout (که در تفکیک ۳تایی مثبت به نظر می‌رسید) در تفکیک ۵تایی منفی شد — نشانه‌ی کلاسیک این‌که نتیجه‌ی «پایدار» قبلی به مرزهای اختیاری تفکیک زیر-بازه حساس بوده، نه یک ادج واقعی.

**این خانواده‌ی سیگنال (`ema_compare_50_200 + atr_pct_percentile گیت`) برای پورت به Provider واقعی توصیه نمی‌شود.** طبق جمع‌بندی [edge-investigation-findings.md](./edge-investigation-findings.md)، گام بعدی منطقی بررسی مسیرهای دیگر است: ترکیب چند provider هم‌راستا، فرضیه‌ی سیگنال متفاوت، یا تایم‌فریم بزرگ‌تر (4h/1d) به‌جای تلاش برای horizonهای طولانی روی 1h که با محدودیت‌های عملیاتی پلتفرم (`max_bars_in_trade`) در تضاد است.

---

## پیگیری: ترکیب چند سیگنال (Ensemble/Majority-Vote) — همان ریشه، مشکل حل نشد

فرضیه‌ی این پیگیری: اگر ناپایداری از رژیم بازار می‌آید (نه از انتخاب آستانه — ردیف #۵ بالا)، شاید معماری Engine-Centric خودِ پلتفرم راه‌حلش را از قبل دارد — `Aggregator` چند Signal Provider مستقل را با رأی‌گیری وزنی ترکیب می‌کند دقیقاً برای همین حالت: وقتی یک سیگنال در یک رژیم خاص اشتباه می‌کند، سیگنال‌های دیگر (با منطق متفاوت) آن را خنثی کنند. این پیگیری همان ایده را یک لایه بالاتر، در سطح research (نه Provider واقعی)، تست کرد.

### روش

ماژول جدید [ensemble_phase.py](../../backend/src/research/ensemble_phase.py) هر ترکیب ۲تایی و ۳تایی (unanimous agreement — رأی‌گیری کاملاً هم‌جهت) از ۶ سیگنال جهت‌دار موجود در `direction_phase.DEFAULT_CANDIDATES` را می‌سازد: `ema_compare_12_26`، `ema_compare_50_200`، `supertrend_direction_10_3.0`، `adx_directional_raw_14`، `adx_directional_gated_14_20`، `market_structure_bias_5`. جمعاً ۳۵ ترکیب (۱۵ دوتایی + ۲۰ سه‌تایی). ابزار اجرا: [run_ensemble_stability_experiment.py](../../backend/scripts/run_ensemble_stability_experiment.py) — همان بازه‌ی داده (BTC/USDT 1h، ۲۰۲۶-۰۱-۰۹ تا ۲۰۲۶-۰۷-۰۹)، همان ۳ زیر-بازه و همان چارچوب `stability_phase.py` (بدون تغییر آن ماژول). هر ترکیب در دو حالت تست شد: **بدون گیت** (خود رأی‌گیری اجماعی، بدون فیلتر نوسان) و **با گیت** (روی `atr_pct_percentile_200`، همان sweep percentile قبلی).

### نتیجه ۱ — بدون گیت: الگوی زیان‌ده مستقل از ترکیب سیگنال، در همه‌ی ۳۵ مورد یکسان است

| زیر-بازه (h=48, ungated) | میانگین net-of-fees expectancy روی ۳۵ ترکیب | تعداد ترکیب‌های مثبت |
|---|---|---|
| ۱/۳ | -0.196% | 10/35 |
| **۲/۳** | **-0.683%** | **0/35** |
| ۳/۳ (holdout) | +0.310% | 35/35 |

**در تمام ۳۵ ترکیب بدون استثنا** — از دوتایی‌های ساده تا سه‌تایی‌های شامل سیگنال‌های کاملاً متفاوت (trend-following مثل EMA/SuperTrend در کنار structural مثل market_structure) — زیر-بازه‌ی وسط منفی و زیر-بازه‌ی سوم مثبت است. این یعنی مشکل ریشه‌ای «تنوع ناکافی سیگنال» نیست: هر ۶ سیگنال پایه، على‌رغم منطق محاسباتی متفاوت (EMA cross، SuperTrend ATR-band، ADX directional index، swing pivot structure)، در نهایت همه از یک خانواده‌ی «پیروی از جهت روند» هستند و در یک رژیم بازار مشخص (احتمالاً choppy/sideways در بازه‌ی زمانی متناظر با زیر-بازه‌ی وسط) همگی هم‌زمان اشتباه می‌کنند. رأی‌گیری اجماعی بین سیگنال‌های با خطای همبسته (correlated errors) — برخلاف فرض اصلی Aggregator — یکدیگر را خنثی نمی‌کند.

### نتیجه ۲ — با گیت percentile: نرخ "پایداری" مشاهده‌شده در سطح شانس تصادفی است

از ۱۴۰ ترکیب×horizon تست‌شده (۳۵ ترکیب × ۴ horizon: 14/24/48/72)، **۳۵ مورد (۲۵٪) `rank_unstable=False` گزارش شد** — در نگاه اول بهتر از تک‌سیگنال (که در بررسی قبلی این سند در ۰٪ موارد پایدار بود). اما این عدد گمراه‌کننده است: با `n_windows=3` فقط **۲ زیر-بازه‌ی train** وجود دارد، یعنی «پایداری رتبه» فقط به این معناست که از ۵ آستانه‌ی کاندید (p75–p95) یک آستانه‌ی واحد هم در زیر-بازه‌ی train اول و هم دوم برنده شود — با ۵ گزینه، احتمال توافق تصادفی صرف حدود ۲۰٪ است. نرخ مشاهده‌شده (۲۵٪) عملاً با سطح شانس یکی است، نه بهبود معنادار. تأیید بیشتر: تقریباً تمام مواردِ «پایدار» روی فقط دو ترکیب (horizon, threshold) خاص متمرکزند — `h=24 @ p75` (۲۴ مورد) و `h=72 @ p95` (۱۱ مورد) — که با الگوی شناخته‌شده‌ی سند (زیر-بازه‌ی سوم/holdout معمولاً مثبت‌تر است، مستقل از آستانه) کاملاً همخوانی دارد، نه با یک ادج خاص آن ترکیب سیگنال.

### جمع‌بندی این پیگیری

| # | یافته | severity | وضعیت |
|---|---|---|---|
| 8 | بدون گیت، در تمام ۳۵ ترکیب ensemble (۲ و ۳ سیگنال) الگوی زیان‌ده در زیر-بازه‌ی وسط عیناً تکرار شد | رد فرضیه‌ی «تنوع سیگنال خطای رژیم را خنثی می‌کند» | ✅ تست شد و رد شد |
| 9 | با گیت، نرخ `rank_unstable=False` (۲۵٪) با سطح توافق تصادفی صرف (~۲۰٪ با ۵ آستانه و ۲ زیر-بازه‌ی train) قابل توضیح است، نه یک بهبود واقعی | ensemble مشکل انتخاب آستانه را حل نمی‌کند | ✅ تست شد و رد شد |

**نتیجه: ترکیب چند سیگنال جهت‌دار موجود (majority-vote/ensemble) این خانواده از ناپایداری رژیمی را حل نمی‌کند.** ریشه‌ی مشکل خطای همبسته بین سیگنال‌هاست، نه کمبود رأی‌گیری — هر ۶ سیگنال پایه در نهایت یک نوع فرضیه (پیروی جهت روند) را با داده‌های قیمتی مشابه می‌سنجند، پس در رژیم‌هایی که این فرضیه بی‌اعتبار است (بازار sideways/choppy) همگی هم‌زمان شکست می‌خورند. این ریشه‌ای‌تر از مسئله‌ی آستانه است: نیاز واقعی یک سیگنال با **فرضیه‌ی متفاوت** است، نه رأی‌گیری بیشتر بین سیگنال‌های هم‌خانواده.

---

## پیگیری: تایم‌فریم بزرگ‌تر (4h، 1d) — رد شد، نتیجه بدتر از 1h بود

فرضیه‌ی این پیگیری (پیشنهاد پایان بخش قبل): رژیم choppy کوتاه‌مدت که هر ۶ سیگنال ensemble را هم‌زمان می‌زند، ویژگی شناخته‌شده‌ی نویز میکرواستراکچر در تایم‌فریم پایین (1h) است؛ رفتن به 4h یا 1d باید این نویز را فیلتر کند بدون نیاز به فرضیه‌ی سیگنال جدید.

### داده و روش

کش‌های از پیش موجود در `data/cache/binance_BTC-USDT_4h.csv` (۳۳۷۹ کندل، ۲۰۲۵-۰۱-۰۱ تا ۲۰۲۶-۰۷-۱۸، ۵۶۳ روز — عریض‌تر از بازه‌ی ۱۸۱روزه‌ای که بررسی‌های قبلی این سند رویش کار می‌کردند) و `binance_BTC-USDT_1d.csv` (۵۶۴ کندل، همان بازه) استفاده شد. همان ابزار `run_ensemble_stability_experiment.py` و همان ۳۵ ترکیب سیگنال، با horizonهای متناسب با هر تایم‌فریم (4h: h=6/12/18/30 بار ≈ ۱ تا ۵ روز؛ 1d: h=3/5/7/10 روز).

### نتیجه — 4h: بدون گیت حتی از 1h هم بی‌ثبات‌تر است

| زیر-بازه (4h, ungated, میانگین روی ۳۵ ترکیب) | h=6 | h=12 | h=18 | h=30 |
|---|---|---|---|---|
| ۱/۳ | -0.555% (0/35) | -0.565% (0/35) | -0.519% (0/35) | -0.462% (1/35) |
| ۲/۳ | -0.244% (0/35) | -0.240% (0/35) | -0.226% (1/35) | -0.403% (0/35) |
| ۳/۳ (holdout) | -0.196% (1/35) | -0.020% (16/35) | +0.190% (27/35) | **+0.636% (35/35)** |

روی 1h، حداقل زیر-بازه‌ی سوم همیشه مثبت بود (۳۵/۳۵) در هر horizon. روی 4h، این فقط در بلندترین horizon تست‌شده (h=30، معادل ۵ روز) اتفاق می‌افتد — در horizonهای کوتاه‌تر (h=6, h=12) **هیچ زیر-بازه‌ای** به‌طور پیوسته مثبت نیست. با گیت percentile هم وضعیت بدتر شد: از ۱۴۰ ترکیب×horizon، فقط **۷ مورد (۵٪) `rank_unstable=False`** — پایین‌تر از سطح شانس تصادفی (~۲۰٪) که در بررسی 1h مشاهده شده بود، نه بالاتر.

### نتیجه — 1d: داده برای نتیجه‌گیری معتبر کافی نیست، اما جهت هم مساعد نبود

با فقط ۵۶۴ کندل روزانه، هر زیر-بازه ۳تایی حدود ۱۳۰-۱۵۰ "معامله" دارد که وقتی گیت percentile هم اضافه شود اکثراً به زیر آستانه‌ی `MIN_TRADES_FOR_COMPARISON` می‌افتد — با گیت، **۰ از ۱۴۰ مورد** `rank_unstable=False` بود (نه به این خاطر که همه ناپایدار بودند، بلکه چون اکثر threshold ها اصلاً trade کافی برای مقایسه نداشتند). بدون گیت، الگو یکنواخت نبود (بر خلاف 1h/4h): زیر-بازه‌ی اول اغلب مثبت (در h=10: 20/35)، ولی زیر-بازه‌ی سوم اغلب و با شدت بیشتر منفی بود (در h=10: میانگین -1.491%، فقط 17/35 مثبت) — دقیقاً برعکس چیزی که 1h/4h نشان می‌دادند. این می‌تواند نویز نمونه‌ی کوچک باشد، نه یک یافته‌ی قابل‌اتکا؛ برای نتیجه‌گیری معتبر روی 1d به تاریخچه‌ی چندسال نیاز است که در این محیط در دسترس نبود.

### جمع‌بندی این پیگیری

| # | یافته | severity | وضعیت |
|---|---|---|---|
| 10 | روی 4h، بدون گیت، هیچ زیر-بازه‌ای در horizonهای کوتاه (۱-۳ روز) به‌طور پیوسته مثبت نیست — بدتر از الگوی 1h | رد فرضیه‌ی «تایم‌فریم بزرگ‌تر نویز رژیمی را فیلتر می‌کند» | ✅ تست شد و رد شد |
| 11 | روی 4h، با گیت، نرخ `rank_unstable=False` (۵٪) پایین‌تر از سطح شانس تصادفی است | نه‌فقط بهبود نداد، از حالت 1h هم بدتر شد | ✅ تست شد و رد شد |
| 12 | روی 1d، داده‌ی موجود (۵۶۴ کندل) برای نتیجه‌گیری آماری معتبر ناکافی است؛ جهت مشاهده‌شده (زیر-بازه‌ی سوم منفی) هم مساعد نبود | نیاز به تاریخچه‌ی چندساله برای تست معتبر 1d | ⚠️ نامعتبر آماری، نه رد قطعی |

**نتیجه: فرضیه‌ی «تایم‌فریم بزرگ‌تر مشکل رژیم choppy را حل می‌کند» رد شد.** روی 4h — تایم‌فریمی که داده‌ی کافی برای تست معتبر داشت — نتیجه نه‌تنها بهتر نشد بلکه به‌وضوح بدتر از 1h بود، هم در حالت ungated و هم gated. این یافته نشان می‌دهد مشکل صرفاً «نویز میکرواستراکچر کوتاه‌مدت» نبود؛ به احتمال بیشتر یک ویژگی ساختاری‌تر بازار BTC/USDT در همان بازه‌ی زمانی (۲۰۲۶) است که در مقیاس‌های زمانی مختلف هم دیده می‌شود، نه فقط در 1h. با رد شدن هر دو مسیر مطرح‌شده (ensemble و تایم‌فریم)، تنها مسیر باقی‌مانده‌ی منطقی طبق [edge-investigation-findings.md](./edge-investigation-findings.md) افزودن یک سیگنال با **فرضیه‌ی بنیادین متفاوت** بود — مثلاً `volume_order_flow`/CMF-style (جریان سفارش، نه جهت قیمت).

---

## پیگیری: افزودن سیگنال order-flow (فرضیه‌ی متفاوت) — رد شد، حتی بدتر از trend-following بود

فرضیه‌ی این پیگیری: تمام ۶ سیگنال تست‌شده تا این‌جا از یک خانواده‌اند (پیروی جهت قیمت/روند)، پس رد شدن ensemble و تایم‌فریم قابل‌پیش‌بینی بود. یک سیگنال با فرضیه‌ی بنیادین متفاوت — جریان سفارش (order flow)، نه جهت قیمت — می‌تواند خطای غیرهمبسته با بقیه داشته باشد و در رژیمی که آن‌ها شکست می‌خورند کار کند.

### روش

`volume_order_flow_direction` به `direction_phase.DEFAULT_CANDIDATES` اضافه شد (`ema_compare_12_26`، `..._50_200`، `supertrend`، `adx` raw/gated، `market_structure_bias_5` را به ۷ سیگنال رساند) — منطقش دقیقاً منطق `providers.volume_order_flow.VolumeOrderFlowProvider` را بازتولید می‌کند (bullish: `CMF >= min_cmf AND volume_ratio >= min_volume_ratio`؛ bearish: قرینه‌ی آن)، با استفاده مستقیم از تابع تولیدی `_volume_flow_components` (بدون بازمحاسبه) طبق قاعده‌ی «یک `DirectionSignal` باید از محاسبات production استفاده کند، نه بازسازی آن‌ها» که سایر کاندیدهای این pool هم رعایت می‌کنند. تنها تفاوت: خروجی این پژوهش UP/DOWN باینری است (نه BUY/SELL/HOLD)، پس روی بار با جریان ضعیف (نه bullish نه bearish) آخرین جهت تأییدشده را carry-forward می‌کند — همان الگویی که `adx_directional_gated` برای بارهای ضعیف خودش استفاده می‌کند.

### نتیجه ۱ — Phase 1 (دقت جهت‌یابی خام): order-flow هم مثل بقیه هیچ ادجی ندارد

روی همان بازه‌ی BTC/USDT 1h (۲۰۲۶-۰۱-۰۹ تا ۲۰۲۶-۰۷-۰۹):

| سیگنال | h=4 | h=8 | h=14 | h=24 |
|---|---|---|---|---|
| `volume_order_flow_20` (جدید) | 50.95% | 50.99% | 51.13% | 52.25% |
| میانگین ۶ سیگنال trend-following قبلی | ~49.6–52.0% | | | |

هیچ‌کدام (شامل order-flow) از آستانه‌ی عبور Phase 1 (`PASS_MARGIN_PCT=2.0pp` در حداقل ۲ horizon) رد نشد — دقت order-flow (۵۰.۹۵–۵۲.۲۵٪) عملاً در همان محدوده‌ی بقیه‌ی سیگنال‌های «no edge» قرار دارد، نه بالاتر.

### نتیجه ۲ — Ensemble با order-flow: نه‌فقط کمک نکرد، وضعیت را بدتر کرد

همان ۳۵ ترکیب قبلی (۲ و ۳ سیگنال، unanimous) به همراه ۲۱ ترکیب جدید شامل `volume_order_flow_20` (جمعاً ۵۶ ترکیب) با همان ابزار و همان بازه دوباره اجرا شد:

| زیر-بازه (ungated، میانگین) | با order-flow در ترکیب (n=21) | بدون order-flow (n=35، قبلی) |
|---|---|---|
| w1 (وسط) | **-0.437% تا -0.650%، ۰/۲۱ مثبت در همه‌ی ۴ horizon** | -0.457% تا -0.703%، ۰/۳۵ مثبت |
| w0 (h=72) | -1.012%، ۰/۲۱ مثبت | -0.389%، ۹/۳۵ مثبت |

زیر-بازه‌ی وسط (w1) با حضور order-flow هنوز **به‌طور کامل منفی** است — دقیقاً همان الگویی که بدون آن دیده می‌شد. بدتر: در h=72، زیر-بازه‌ی اول (w0) با حضور order-flow از ۹/۳۵ مثبت (بدون آن) به ۰/۲۱ مثبت سقوط کرد — یعنی افزودن order-flow به ترکیب، به‌جای خنثی کردن رژیم بد، حداقل در یک horizon آن را تشدید کرد.

با گیت percentile هم همین الگو: نرخ `rank_unstable=False` برای ترکیب‌های شامل order-flow فقط **۳.۶٪ (۳/۸۴)** بود — پایین‌تر از ترکیب‌های بدون آن (۲۳.۶٪، ۳۳/۱۴۰) که خودش هم نزدیک سطح شانس تصادفی بود.

### جمع‌بندی این پیگیری

| # | یافته | severity | وضعیت |
|---|---|---|---|
| 13 | `volume_order_flow_20` در Phase 1 (دقت جهت‌یابی خام) هیچ ادجی نشان نداد — در محدوده‌ی همان ۶ سیگنال «no edge» قبلی | رد فرضیه‌ی «order-flow ادج مستقل قوی‌تری دارد» | ✅ تست شد و رد شد |
| 14 | در ensemble، زیر-بازه‌ی وسط با حضور order-flow هنوز ۰٪ از ترکیب‌ها مثبت بود — دقیقاً مثل بدون آن | رد فرضیه‌ی «order-flow خطای غیرهمبسته با trend-following دارد» | ✅ تست شد و رد شد |
| 15 | نرخ پایداری گیت با order-flow (۳.۶٪) پایین‌تر از بدون آن (۲۳.۶٪) بود — افزودنش وضعیت را بدتر کرد، نه بهتر | order-flow نه‌فقط کمک نکرد، مضر بود | ✅ تست شد و رد شد |

**نتیجه: فرضیه‌ی order-flow هم رد شد.** این نشان می‌دهد ریشه‌ی مشکل عمیق‌تر از «همه‌ی سیگنال‌های موجود از یک خانواده‌اند» است — حتی سیگنالی با منطق محاسباتی کاملاً متفاوت (CMF/حجم، نه قیمت/EMA/pivot) در همان زیر-بازه‌ی زمانی شکست می‌خورد.

---

## بررسی زیرساخت: آیا نتایج منفی نشانه‌ی باگ در pipeline تحقیق هستند؟ — نه؛ ریشه پیدا و توضیح داده شد

بعد از چهار رد پیاپی روی **دقیقاً همان زیر-بازه‌ی وسط** (بندهای بالا)، این سؤال به‌جا مطرح شد: آیا این تکرار نشانه‌ی یک باگ سیستماتیک در خط لوله‌ی تحقیق است (مثلاً محاسبه‌ی نادرست `fwd_ret`، داده‌ی cache خراب، یا خطای علامت در `_trade_pnls`)، نه یک یافته‌ی واقعی بازار؟ این احتمال مستقیماً بررسی شد.

### گام ۱ — رفتار قیمت واقعی در هر زیر-بازه

```
window 0 (۲۰۲۶-۰۱-۰۹ تا ۲۰۲۶-۰۳-۱۰): 90,449 → 70,236   (-22.3%، روند نزولی)
window 1 (۲۰۲۶-۰۳-۱۰ تا ۲۰۲۶-۰۵-۱۰): 70,236 → 80,760   (+15.0%، روند صعودی)
window 2 (۲۰۲۶-۰۵-۱۰ تا ۲۰۲۶-۰۷-۰۹): 80,760 → 62,727   (-22.3%، روند نزولی)
```

زیر-بازه‌ی «بد» (window 1) در واقعیت یک **روند صعودی خالص و قوی +۱۵٪** بوده — نه یک بازار آشفته یا بی‌روند که انتظار ضعف سیگنال‌های trend-following را توجیه کند، و قطعاً نه نشانه‌ی داده‌ی خراب.

### گام ۲ — چرا یک سیگنال trend-following در یک روند صعودی ضرر می‌دهد؟

تجزیه‌ی PnL سیگنال `ema_compare_50_200` روی همین window با h=48 (۱۴۴۸ بار، بدون گیت):

| سمت سیگنال | تعداد بار | میانگین PnL هر معامله | مجموع PnL |
|---|---|---|---|
| UP (long) | 977 (۶۷.۵٪) | **+0.540%** | +528.0% |
| DOWN (short) | 471 (۳۲.۵٪) | **-0.496%** | -233.7% |
| **مجموع (gross)** | 1448 | **+0.203%** | +294.3% |
| **مجموع (net-of-fees)** | 1448 | **-0.097%** | — |

سمت UP سودده بود (همان‌طور که باید در یک روند صعودی باشد)، اما سمت DOWN — که به‌خاطر whipsaw طبیعی EMA در ۳۲.۵٪ از بارها فعال شد، حتی وسط یک روند خالص صعودی — زیان‌ده بود. مجموع gross هنوز اندکی مثبت (+۰.۲۰۳٪) بود، اما هزینه‌ی رفت‌وبرگشت واقعی معامله (`load_default_fill_model()`: fee ۱۰bps + slippage ۵bps هر طرف = **۰.۳٪ round-trip**) این ادج کوچک را کاملاً بلعید و expectancy را منفی کرد.

### گام ۳ — تأیید صحت محاسبات پایه (نه صرفاً فرض)

- `fwd_ret_48` در یک بار دلخواه با محاسبه‌ی دستی `(close[t+48]/close[t]-1)*100` مطابقت دقیق داشت — بدون نگاه‌به‌جلو.
- `load_default_fill_model()` مقدار واقعی از `config/settings.yaml` می‌خواند: `next_open_v1`، fee=10bps، slippage=5bps — همان مدلی که در بخش «بررسی ریشه‌ای» ([edge-investigation-findings.md](./edge-investigation-findings.md)) به‌عنوان فیکس look-ahead ثبت شده.

### جمع‌بندی این بررسی

| # | یافته | severity | وضعیت |
|---|---|---|---|
| 16 | زیر-بازه‌ی «بد» یک روند صعودی +۱۵٪ واقعی بود، نه داده‌ی خراب یا bug — رد فرضیه‌ی «باگ زیرساختی» | رفع نگرانی اعتبار pipeline | ✅ زیرساخت تأیید شد، سالم است |
| 17 | علت واقعی ضرر: whipsaw طبیعی (۳۲.۵٪ بارها DOWN حتی در روند صعودی خالص) gross edge را کوچک نگه می‌دارد (+۰.۲۰۳٪)، و هزینه‌ی رفت‌وبرگشت معامله (۰.۳٪) همین ادج کوچک را می‌بلعد | این یافته‌ی اصلی — نه پیدا کردن مشکل، بلکه تأیید علت آماری واقعی رد شدن | ✅ علت ریشه‌ای شناسایی و کمی‌سازی شد |

**نتیجه: نتایج منفی مکرر این خط تحقیق نشانه‌ی باگ زیرساخت نیستند — نشانه‌ی یک یافته‌ی آماری واقعی و قابل‌توضیح‌اند.** سیگنال‌های کلاسیک trend-following روی BTC/USDT 1h یک gross edge دارند، اما این ادج به‌قدری کوچک است (در حد چند ده صدم درصد هر معامله) که هزینه‌ی واقعی معامله (fee+slippage) آن را می‌بلعد. این با یافته‌ی زیربنایی `edge-investigation-findings.md` («بازار BTC/USDT در 1h به‌شدت رقابتی است») کاملاً سازگار است و آن را با شواهد عددی مستقیم (نه فقط استدلال کیفی) تقویت می‌کند. تلاش‌های بعدی برای یافتن ادج پایدار روی این بازار/تایم‌فریم باید این محدودیت را به‌رسمیت بشناسند: مشکل «کدام سیگنال یا آستانه» نیست، مشکل بزرگی نسبی gross edge در برابر هزینه‌ی تراکنش است — که فقط با تعداد معامله‌ی کمتر (تایم‌فریم بزرگ‌تر — که تست و رد شد چون gross edge هم با آن کوچک‌تر شد) یا یک منبع ادج اساساً بزرگ‌تر (نه از همین خانواده‌ی سیگنال‌های عمومی و شناخته‌شده) قابل حل است.
